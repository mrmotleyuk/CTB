<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title> Custom Tone Builder </title>
  </head>

  <body>
    <div class="H1">
      Test Your Hearing
    </div>
    <div class="p1">
      Average human hearing range is between 20Hz and 20,000Hz (20KHz).<br>
      This drops to around 16KHz in your 30's and down to about 12KHz in your 50's.<br>
      Use your mouse to adjust the slider and the arrow keys of your keyboard to fine tune the frequency.
    </div>
    <div class="slidecontainer">
      <input type="range" min="0" max="25000" value="0" class="slider" id="myRange">
      Current frequency:<span class="freqval" id="frequencyValue"></span> Hz
    </div>
    <script>
      var audioCtx = new(window.AudioContext || window.webkitAudioContext)();
      var sinea = audioCtx.createOscillator();
      sinea.frequency.value = 0;
      sinea.type = "sine";
      sinea.connect(audioCtx.destination);
      sinea.start();

      var slider = document.getElementById("myRange");
      slider.addEventListener("input", function() {
        sinea.frequency.value = slider.value;
        document.getElementById("frequencyValue").innerHTML = slider.value;
      });

    </script>
  </body>

</html>
